# Hotkey Compatibility

Global hotkeys are **not implemented** in this prototype. The application exposes a disabled
`HotkeyService` and continues to provide GUI playback on every platform.

Future support must document macOS Accessibility/Input Monitoring requirements and must not claim
universal Linux Wayland global-hotkey support.
